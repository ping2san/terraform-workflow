---
# This is a test gitflow job
name: hello world

on: [pull_request,push]

jobs:
  hello-world-setup:
    runs-on: ubuntu-latest
    steps:
      - run: ./scripts/file.sh

  hello-world-build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: echo $HOSTNAME-"Hello World"
  
  